# FILE: requirements.txt

# This is the "Golden Trio" of pinned versions that are guaranteed
# to be compatible with each other and resolve all previous errors.
numpy==1.26.4
pyarrow==14.0.1
datasets==2.14.5

# These are the rest of our application's packages.
# Pip will choose versions of these that work with the Golden Trio.
evaluate
torch
transformers
langchain
langchain-google-genai
pandas
streamlit
playwright==1.49.0
python-dotenv
